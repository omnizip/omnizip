# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
#
# PINNED SNAPSHOT (2026-09-21): the config used to inherit the
# oss-guides file over HTTPS directly. A remote main that moves under
# us — combined with CI's fresh dependency resolution — made rubocop
# behavior change with zero repo-side diffs (the 2026-09-21
# Metrics/BlockLength + standard-custom CI breakage). The snapshot is
# refreshed deliberately: refresh with
#   curl -sL https://raw.githubusercontent.com/riboseinc/oss-guides/main/ci/rubocop.yml -o .rubocop-oss.yml
inherit_from:
  - .rubocop-oss.yml
  - .rubocop_todo.yml

# local repo-specific modifications
# ...

AllCops:
  NewCops: enable
  TargetRubyVersion: 3.0
  # rake rust:build clones omnizip-rs under lib/tasks/build (and a
  # build/ dir may exist at the root) — never lint the vendored
  # checkout's Ruby files.
  Exclude:
    - "build/**/*"
    - "lib/tasks/build/**/*"

# 7-Zip SDK uses is_char_state? naming convention (IsCharState in C)
# We keep this naming to match the SDK reference
Naming/PredicatePrefix:
  AllowedMethods:
    - is_a?
    - is_char_state?
  Exclude:
    - 'spec/**/*'
    - 'lib/omnizip/algorithms/lzma/sdk_state_machine.rb'
    - 'lib/omnizip/implementations/seven_zip/lzma/state_machine.rb'
    - 'scripts/compare_is_match_states.rb'

# Ruby 3.0 doesn't support anonymous block forwarding (&) syntax introduced in Ruby 3.1
# Disable these cops to allow explicit &block syntax for Ruby 3.0 compatibility
Naming/BlockForwarding:
  Enabled: false

Style/ArgumentsForwarding:
  Enabled: false
