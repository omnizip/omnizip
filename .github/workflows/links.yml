name: Link Checker

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'readme-docs/**'
      - 'README.adoc'
  pull_request:
    paths:
      - 'docs/**'
      - 'readme-docs/**'
      - 'README.adoc'
  schedule:
    # Run weekly on Sunday at 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  link_checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true

      - name: Build site (Coradoc, no Jekyll)
        run: bundle exec rake docs:build


      - name: Restore lychee cache
        uses: actions/cache@v4
        with:
          path: .lycheecache
          key: cache-lychee-${{ github.sha }}
          restore-keys: cache-lychee-

      - name: Check if site was built
        run: |
          if [ ! -d "docs/site" ]; then
            echo "Error: docs/site directory not created"
            exit 1
          fi
          echo "Site built successfully"
          ls -la docs/site/

      - name: Link Checker
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --verbose
            --no-progress
            --config docs/lychee.toml
            --root-dir "$(pwd)/docs/site"
            --base-url file://${{ github.workspace }}/docs/site/
            'docs/site/**/*.html'
          fail: true
          output: link-check-results.md
          format: markdown
          workingDirectory: .

      - name: Upload link check results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: link-check-results
          path: link-check-results.md
          retention-days: 30

      - name: Comment PR with results
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let comment = '## 🔗 Link Check Failed\n\n';

            if (fs.existsSync('link-check-results.md')) {
              const results = fs.readFileSync('link-check-results.md', 'utf8');
              comment += '### Results\n\n' + results + '\n\n';
            }

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });